# Semantic-pack candidate pricing

Generated by `bench/semantic_pack/pricing.py`. Corpus matches are queue signals, not proof.

## Summary

- Iterations: 20
- Priced ready: 1
- Priced but blocked: 9
- Unpriced: 10
- Repos available: 120 / 120
- Corpus digest: `d152e7150a08d662`
- Candidate signature: `3cb6e2757179dd9c`

## Iterations

| # | candidate | verdict | repos | heldout | next action |
|---:|---|---|---:|---:|---|
| 1 | `java.guava.immutable_collection_factories` | priced-ready | 3 | 0 | Keep builtin-default gate evidence and Guava hard negatives current with implementation PRs. |
| 2 | `java.guava.optional_presence` | priced-but-blocked | 2 | 0 | Record proof prerequisite. |
| 3 | `javascript.lodash.collection_projection` | unpriced | 0 | 0 | Retain only as seed-list context until corpus evidence appears. |
| 4 | `javascript.lodash.membership_predicates` | unpriced | 0 | 0 | Retain only as seed-list context until corpus evidence appears. |
| 5 | `python.numpy.scalar_integer_ufuncs` | priced-but-blocked | 1 | 1 | Record dtype/scalar-domain blocker. |
| 6 | `python.numpy_clip_minmax` | unpriced | 0 | 0 | Retain only as seed-list context until corpus evidence appears. |
| 7 | `python.pandas_series_null_predicates` | unpriced | 0 | 0 | Retain only as seed-list context until corpus evidence appears. |
| 8 | `python.pandas_fillna_defaults` | unpriced | 0 | 0 | Retain only as seed-list context until corpus evidence appears. |
| 9 | `javascript.rxjs_observable_identity_adapters` | priced-but-blocked | 1 | 1 | Record Observable demand/effect blocker. |
| 10 | `javascript.rxjs_projection_operators` | priced-but-blocked | 3 | 2 | Record lifecycle/callback blocker. |
| 11 | `rust.tokio_future_identity_adapters` | unpriced | 4 | 3 | Reject this broad Tokio runtime-boundary signal; re-open only after a concrete adapter row is isolated. |
| 12 | `rust.tokio_stream_projection` | unpriced | 1 | 1 | Reject current signal until receiver-proximate StreamExt calls can be isolated. |
| 13 | `ruby.rails_active_support_presence` | priced-but-blocked | 1 | 0 | Record monkey-patch/version blocker. |
| 14 | `ruby.rails_collection_helpers` | unpriced | 0 | 0 | Retain only as seed-list context until corpus evidence appears. |
| 15 | `python.pathlib_path_predicates` | unpriced | 10 | 5 | Reject stateful predicate rows; maybe split pure path normalization later. |
| 16 | `python.itertools_chain_factories` | priced-but-blocked | 13 | 7 | Record laziness/demand blocker. |
| 17 | `javascript.node_path_normalization` | unpriced | 15 | 5 | Reject broad Node path row; split literal POSIX-only candidate if evidence appears. |
| 18 | `go.stdlib.maps_helpers` | priced-but-blocked | 8 | 4 | Record Go version/order blocker. |
| 19 | `java.apache_commons_lang_string_utils` | priced-but-blocked | 1 | 0 | Require external consumer or held-out corpus evidence before target packet. |
| 20 | `rust.itertools_collect_vec` | priced-but-blocked | 2 | 1 | Record trait/import proof blocker. |

## Details

### 1. `java.guava.immutable_collection_factories`

- Verdict: `priced-ready` — Corpus signal plus existing vocabulary justify a narrow implementation packet.
- Proposed pack: `nose.java.ecosystem.guava.immutable_collection_factories`
- Row slice: ImmutableList.of, ImmutableSet.of, and ImmutableMap.of immutable factory calls
- Corpus signal: 3 repos, 0 held-out repos, 2649 raw occurrences
- Current detector/query result: Implemented as the builtin-default Guava immutable collection factory pack; copyOf and broad Guava surfaces remain closed.
- Product query sample: sample product query overlay plus existing semantic-pack inventory knowledge; candidate pack observed: True
- Impact: High: common Java corpus ecosystem and direct collection-factory equivalence value.
- Safety: Medium: can reuse Java collection-factory vocabulary, but exact package and overload proof must be explicit.
- Hard negatives: wrong package with same class name, shadowed ImmutableList/Set/Map, unsupported ImmutableMap.of arity, static null element/key/value, duplicate static ImmutableMap key, copyOf remains unsupported
- Samples:
  - `gson:proto/src/main/java/com/google/gson/protobuf/LegacyProtoTypeAdapterFactory.java:135` (guava_immutable_set) — final ImmutableSet<FieldNamingPolicy> SUPPORTED_FIELD_NAMING_POLICIES = ImmutableSet.of( FieldNamingPolicy.IDENTITY, FieldNamingPolicy.UPPER_CAM
  - `gson:proto/src/test/java/com/google/gson/protobuf/functional/LegacyProtoTypeAdapterFactoryTest.java:235` (guava_immutable_set) — ual. */ private static final ImmutableSet<String> TRANSIENT_FIELDS = ImmutableSet.of("memoizedHashCode", "memoizedIsInitialized", "memoizedSize", "unknownFields");
  - `gson:proto/src/test/java/com/google/gson/protobuf/functional/LegacyProtoTypeAdapterFactoryTest.java:237` (guava_immutable_set) — nownFields"); private static final ImmutableSet<String> BYTE_STRING_FIELDS = ImmutableSet.of("bytes", "hash"); // "memoizedIsInitialized":1,"memoizedSize":-1,"memoizedHas

### 2. `java.guava.optional_presence`

- Verdict: `priced-but-blocked` — Impact exists, but exact influence is blocked by the recorded proof/evidence prerequisite.
- Proposed pack: `nose.java.ecosystem.guava.optional`
- Row slice: Optional.absent/fromNullable/isPresent/or/default-style presence slices
- Corpus signal: 2 repos, 0 held-out repos, 349 raw occurrences
- Current detector/query result: Current option/null presence support does not prove Guava Optional identity.
- Product query sample: sample product query overlay plus existing semantic-pack inventory knowledge; candidate pack observed: False
- Impact: Medium: visible where legacy Guava Optional predates java.util.Optional.
- Safety: High: nullability and default semantics differ from java.util.Optional and require version/package proof.
- Hard negatives: java.util.Optional, shadowed Optional, or(default) with effectful default, fromNullable vs of null behavior
- Blocked by: Guava Optional version/package proof, default-demand/effect profile
- Samples:
  - `guava:android/guava/src/com/google/common/base/Absent.java:50` (guava_optional) — ublic T or(T defaultValue) { return checkNotNull(defaultValue, "use Optional.orNull() instead of Optional.or(null)"); } @SuppressWarnings("unchecked") // safe
  - `guava:android/guava/src/com/google/common/base/Absent.java:62` (guava_optional) — ds T> supplier) { return checkNotNull( supplier.get(), "use Optional.orNull() instead of a Supplier that returns null"); } @Override public @Nullable
  - `guava:android/guava/src/com/google/common/base/Absent.java:78` (guava_optional) — sform(Function<? super T, V> function) { checkNotNull(function); return Optional.absent(); } @Override public boolean equals(@Nullable Object obj) { return t

### 3. `javascript.lodash.collection_projection`

- Verdict: `unpriced` — No corpus presence in the available pinned checkouts; retain only as seed-list context.
- Proposed pack: `nose.javascript.ecosystem.lodash.collection_projection`
- Row slice: map/filter/some/every over arrays with explicit callbacks
- Corpus signal: 0 repos, 0 held-out repos, 0 raw occurrences
- Current detector/query result: Current JS array/HOF facts do not admit Lodash selectors as builtin evidence.
- Product query sample: static pricing overlay plus existing semantic-pack inventory knowledge; candidate pack observed: False
- Impact: High if present: projection/predicate helpers are high-value Type-4 surfaces.
- Safety: High: iteratee shorthands, object iteration order, lazy chains, and callback effects need hard boundaries.
- Hard negatives: iteratee shorthand, object receiver, lazy chain, custom underscore binding, effectful callback
- Rejection context: No corpus presence in the available pinned checkouts; retain only as seed-list context.

### 4. `javascript.lodash.membership_predicates`

- Verdict: `unpriced` — No corpus presence in the available pinned checkouts; retain only as seed-list context.
- Proposed pack: `nose.javascript.ecosystem.lodash.membership`
- Row slice: includes/has/some membership slices
- Corpus signal: 0 repos, 0 held-out repos, 0 raw occurrences
- Current detector/query result: Current receiver-membership protocol does not include Lodash package semantics.
- Product query sample: static pricing overlay plus existing semantic-pack inventory knowledge; candidate pack observed: False
- Impact: Medium: membership is common and maps to existing protocol vocabulary when receiver proof exists.
- Safety: High: strings, objects, arrays, path keys, and SameValueZero behavior diverge.
- Hard negatives: string substring includes, object path lookup, custom lodash binding, NaN/-0 boundary, iteratee predicate some
- Rejection context: No corpus presence in the available pinned checkouts; retain only as seed-list context.

### 5. `python.numpy.scalar_integer_ufuncs`

- Verdict: `priced-but-blocked` — Impact exists, but exact influence is blocked by the recorded proof/evidence prerequisite.
- Proposed pack: `nose.python.ecosystem.numpy.scalar_integer_ufuncs`
- Row slice: np.abs/minimum/maximum over proven scalar integer values
- Corpus signal: 1 repos, 1 held-out repos, 8 raw occurrences
- Current detector/query result: Current numeric min/max/abs support is scalar language/stdlib, not NumPy package semantics.
- Product query sample: sample product query overlay plus existing semantic-pack inventory knowledge; candidate pack observed: False
- Impact: Observed corpus evidence is NumPy ufunc presence only; scalar integer impact remains blocked until value-domain proof exists.
- Safety: High: dtype, broadcasting, NaN, signed zero, overflow, and ndarray mutation/view semantics are outside current scalar proof.
- Hard negatives: ndarray broadcasting, float NaN/signed-zero, object dtype, in-place out parameter, shadowed np
- Blocked by: dtype/domain proof, array-vs-scalar boundary
- Samples:
  - `sympy:sympy/external/tests/test_autowrap.py:81` (numpy_scalar_numpy) — 20) x = numpy.random.rand(20) y = numpy.dot(M, x) assert numpy.sum(numpy.abs(y - mv(M, x))) < 1e-13 def runtest_autowrap_matrix_matrix(language, backend):
  - `sympy:sympy/external/tests/test_autowrap.py:93` (numpy_scalar_numpy) — M2 = numpy.random.rand(20, 15) M3 = numpy.dot(M1, M2) assert numpy.sum(numpy.abs(M3 - matmat(M1, M2))) < 1e-13 def runtest_ufuncify(language, backend): has
  - `sympy:sympy/plotting/experimental_lambdify.py:134` (numpy_scalar_np) — ctor_func(*temp_args) results = np.ma.masked_where( np.abs(results.imag) > 1e-7 * np.abs(results), results.real, copy=False

### 6. `python.numpy_clip_minmax`

- Verdict: `unpriced` — No corpus presence in the available pinned checkouts; retain only as seed-list context.
- Proposed pack: `nose.python.ecosystem.numpy.clip`
- Row slice: np.clip and min(max(x, lo), hi) only under proven scalar integer bounds
- Corpus signal: 0 repos, 0 held-out repos, 0 raw occurrences
- Current detector/query result: Current clamp law is value-graph scalar/integer, not NumPy ufunc semantics.
- Product query sample: static pricing overlay plus existing semantic-pack inventory knowledge; candidate pack observed: False
- Impact: Medium-high if scalar instances exist; aligns with existing clamp proof shape.
- Safety: High: array broadcasting, dtype, NaN, and bound ordering can invalidate exact equivalence.
- Hard negatives: unproven bound order, float/NaN, array input, broadcasted bounds, out parameter
- Rejection context: No corpus presence in the available pinned checkouts; retain only as seed-list context.

### 7. `python.pandas_series_null_predicates`

- Verdict: `unpriced` — No corpus presence in the available pinned checkouts; retain only as seed-list context.
- Proposed pack: `nose.python.ecosystem.pandas.series_null_predicates`
- Row slice: Series.isna/notna/isnull/notnull predicates
- Corpus signal: 0 repos, 0 held-out repos, 0 raw occurrences
- Current detector/query result: Current null presence facts are scalar/control-flow, not vectorized pandas masks.
- Product query sample: static pricing overlay plus existing semantic-pack inventory knowledge; candidate pack observed: False
- Impact: Medium in pandas-heavy repos, but current pinned corpus may be sparse.
- Safety: Very high: index alignment, NA value model, dtype, mask shape, and view/copy behavior are unmodeled.
- Hard negatives: DataFrame receiver, object dtype, index alignment, view/copy mutation, shadowed pandas
- Rejection context: No corpus presence in the available pinned checkouts; retain only as seed-list context.

### 8. `python.pandas_fillna_defaults`

- Verdict: `unpriced` — No corpus presence in the available pinned checkouts; retain only as seed-list context.
- Proposed pack: `nose.python.ecosystem.pandas.fillna_defaults`
- Row slice: Series/DataFrame fillna and defaulting slices
- Corpus signal: 0 repos, 0 held-out repos, 0 raw occurrences
- Current detector/query result: Current map/default and nullish-default facts are scalar/map, not pandas vector operations.
- Product query sample: static pricing overlay plus existing semantic-pack inventory knowledge; candidate pack observed: False
- Impact: Medium where pandas appears, but refactoring value needs manual confirmation.
- Safety: Very high: index alignment, inplace mutation, dtype coercion, NA semantics, and method options change behavior.
- Hard negatives: inplace=True, method/limit/axis arguments, DataFrame alignment, nullable dtypes, view/copy boundary
- Rejection context: No corpus presence in the available pinned checkouts; retain only as seed-list context.

### 9. `javascript.rxjs_observable_identity_adapters`

- Verdict: `priced-but-blocked` — Impact exists, but exact influence is blocked by the recorded proof/evidence prerequisite.
- Proposed pack: `nose.javascript.ecosystem.rxjs.observable_identity_adapters`
- Row slice: identity-like pipe/map/filter slices
- Corpus signal: 1 repos, 1 held-out repos, 17 raw occurrences
- Current detector/query result: Current promise/async facts do not model Observable demand/effect.
- Product query sample: sample product query overlay plus existing semantic-pack inventory knowledge; candidate pack observed: False
- Impact: Medium-high in reactive TypeScript repos.
- Safety: High: scheduler, subscription timing, hot/cold streams, completion, error, and cancellation are semantic boundaries.
- Hard negatives: hot vs cold stream, scheduler argument, side-effecting operator, error/completion changes, custom Observable
- Blocked by: Observable demand/effect substrate, scheduler boundary
- Samples:
  - `rxjs:packages/rxjs/spec/operators/filter-spec.ts:428` (rxjs_filter_boolean_ts) — ; const expected = ' --t---t----t-------|'; expectObservable(e1.pipe(filter(Boolean))).toBe(expected, values); expectSubscriptions(e1.subscriptions).toBe(e1su
  - `rxjs:packages/rxjs/spec/operators/mergeMap-spec.ts:237` (rxjs_identity_map_ts) — ----------------------------------------------------!'; const source = e1.pipe( map((x) => x), mergeMap(() => x), map((x) => x) ); expectObserva
  - `rxjs:packages/rxjs/spec/operators/mergeMapTo-spec.ts:191` (rxjs_identity_map_ts) — ----------------------------------------------------!'; const result = e1.pipe( map((x) => x), mergeMapTo(x), map((x) => x) ); expectObservable(

### 10. `javascript.rxjs_projection_operators`

- Verdict: `priced-but-blocked` — Impact exists, but exact influence is blocked by the recorded proof/evidence prerequisite.
- Proposed pack: `nose.javascript.ecosystem.rxjs.projection_operators`
- Row slice: map/filter/take projection and predicate operators inside pipe
- Corpus signal: 3 repos, 2 held-out repos, 373 raw occurrences
- Current detector/query result: No current exact Observable operator contract.
- Product query sample: sample product query overlay plus existing semantic-pack inventory knowledge; candidate pack observed: False
- Impact: Medium where RxJS/trpc-style reactive code is present.
- Safety: High: callback effects and stream lifecycle make array-HOF analogies unsafe.
- Hard negatives: tap side effects, switchMap/mergeMap flattening, throwError/catchError, scheduler-dependent timing
- Blocked by: stream lifecycle proof, callback effect proof
- Samples:
  - `prettier:tests/format/js/functional-composition/rxjs_pipe.js:6` (rxjs_pipe_operator_js) — p, filter, scan } from 'rxjs/operators'; const source$ = range(0, 10); source$.pipe( filter(x => x % 2 === 0), map(x => x + x), scan((acc, x) => acc + x, 0) ) .subscribe(x => console.log(x))
  - `rxjs:apps/rxjs.dev/src/app/app.component.ts:233` (rxjs_pipe_operator_ts) — this.navigationService.versionInfo, this.navigationService.navigationViews.pipe(map((views) => views.docVersions)) ).subscribe(([versionInfo, versions]) => {
  - `rxjs:apps/rxjs.dev/src/app/app.component.ts:254` (rxjs_pipe_operator_ts) — => (this.versionInfo = vi)); const hasNonEmptyToc = this.tocService.tocList.pipe(map((tocList) => tocList.length > 0)); combineLatest(hasNonEmptyToc, this.showFl

### 11. `rust.tokio_future_identity_adapters`

- Verdict: `unpriced` — The matcher prices Tokio spawn/test/runtime boundaries, not identity adapter equivalence.
- Proposed pack: `nose.rust.ecosystem.tokio.future_identity_adapters`
- Row slice: tokio::spawn/select/join/test boundaries as non-equivalence corpus signal
- Corpus signal: 4 repos, 3 held-out repos, 2187 raw occurrences
- Current detector/query result: Current async/future facts do not treat Tokio task/runtime boundaries as exact semantic evidence.
- Product query sample: sample product query overlay plus existing semantic-pack inventory knowledge; candidate pack observed: False
- Impact: High prevalence in async Rust repos, but this is runtime-boundary prevalence rather than row-sized semantic-equivalence impact.
- Safety: High: scheduler, cancellation, pinning, wake order, and side effects are unmodeled.
- Hard negatives: spawn boundary, select race, timeout/cancellation, side-effecting future, pin/projection boundary
- Rejection context: The matcher prices Tokio spawn/test/runtime boundaries, not identity adapter equivalence.
- Samples:
  - `meilisearch:crates/http-client/src/lib.rs:73` (tokio_spawn) — assert_eq!(format!("{origin:?}"), "DeniedLocalIp"); }) } #[tokio::test] async fn reqwest_mapped_ipv4() { let client = crate::reqwest::
  - `meilisearch:crates/index-scheduler/src/scheduler/enterprise_edition/network.rs:459` (tokio_spawn) — y care when this task finishes so we can detach it and let it live. tokio::spawn(request); } Ok(()) } #[allow(clippy::too_many_argumen
  - `meilisearch:crates/index-scheduler/src/scheduler/enterprise_edition/s3.rs:133` (tokio_spawn) — let ip_policy = self.scheduler.ip_policy.clone(); let uploader_task = tokio::spawn(async move { let (s3_access_key, s3_secret_key, s3_token) = Self::e

### 12. `rust.tokio_stream_projection`

- Verdict: `unpriced` — Current corpus signal is too easy to pollute with non-stream Option/Result/Iterator calls.
- Proposed pack: `nose.rust.ecosystem.tokio.stream_projection`
- Row slice: StreamExt map/filter/then identity or projection slices
- Corpus signal: 1 repos, 1 held-out repos, 16 raw occurrences
- Current detector/query result: Current Rust iterator adapters are exact only for iterator identity, not async streams.
- Product query sample: sample product query overlay plus existing semantic-pack inventory knowledge; candidate pack observed: False
- Impact: Medium if stream-heavy repos expose repeated projection idioms.
- Safety: High: poll order, pending, cancellation, and effectful closures differ from iterator semantics.
- Hard negatives: effectful closure, then async boundary, filter_map absence semantics, custom map method
- Rejection context: Current corpus signal is too easy to pollute with non-stream Option/Result/Iterator calls.
- Samples:
  - `tokio:examples/connect-tcp.rs:57` (tokio_stream_ext) — d the stream let mut stream = FramedRead::new(r, BytesCodec::new()) .filter_map(|i| match i { //BytesMut into Bytes Ok(i) => future::rea
  - `tokio:tokio-stream/src/stream_ext.rs:296` (tokio_stream_ext) — } /// /// let stream = stream::iter(1..=3); /// let stream = stream.then(do_async_work); /// /// tokio::pin!(stream); /// /// assert_eq!(
  - `tokio:tokio-stream/src/stream_ext.rs:463` (tokio_stream_ext) — /// /// let stream = stream::iter(1..=8); /// let mut evens = stream.filter_map(|x| { /// if x % 2 == 0 { Some(x + 1) } else { None } /// }); //

### 13. `ruby.rails_active_support_presence`

- Verdict: `priced-but-blocked` — Impact exists, but exact influence is blocked by the recorded proof/evidence prerequisite.
- Proposed pack: `nose.ruby.ecosystem.rails.active_support_presence`
- Row slice: present?/blank?/presence slices
- Corpus signal: 1 repos, 0 held-out repos, 3 raw occurrences
- Current detector/query result: Current Ruby nil/empty facts do not include ActiveSupport monkey-patched semantics.
- Product query sample: sample product query overlay plus existing semantic-pack inventory knowledge; candidate pack observed: False
- Impact: Medium in Rails repos; useful only when receiver classes are constrained.
- Safety: High: monkey patching, nil/empty/string whitespace behavior, and receiver class differ by ActiveSupport version.
- Hard negatives: custom present?/blank?, non-ActiveSupport environment, String whitespace, relation/query receivers
- Blocked by: ActiveSupport version proof, receiver class proof
- Samples:
  - `devise:lib/devise/models/rememberable.rb:76` (rails_presence) — elsif respond_to?(:authenticatable_salt) && (salt = authenticatable_salt.presence) salt else raise "authenticatable_salt returned nil
  - `devise:lib/devise/strategies/authenticatable.rb:159` (rails_presence) — lues(hash, keys) keys.each do |key, enforce| value = hash[key].presence if value self.authentication_hash[key] = value
  - `devise:lib/generators/devise/views_generator.rb:57` (rails_presence) — e || :devise}" end def plural_scope @plural_scope ||= scope.presence && scope.underscore.pluralize end end class SharedViewsGenerator

### 14. `ruby.rails_collection_helpers`

- Verdict: `unpriced` — No corpus presence in the available pinned checkouts; retain only as seed-list context.
- Proposed pack: `nose.ruby.ecosystem.rails.collection_helpers`
- Row slice: many?/exclude?/including?/excluding? collection helper rows
- Corpus signal: 0 repos, 0 held-out repos, 0 raw occurrences
- Current detector/query result: Current Ruby collection membership/factory facts do not model ActiveSupport helpers.
- Product query sample: static pricing overlay plus existing semantic-pack inventory knowledge; candidate pack observed: False
- Impact: Low-medium unless repeated in Rails-heavy held-out repos.
- Safety: High: monkey-patched methods and ActiveRecord relation laziness make exact support risky.
- Hard negatives: ActiveRecord::Relation, custom helper method, lazy query, mutating helper behavior
- Rejection context: No corpus presence in the available pinned checkouts; retain only as seed-list context.

### 15. `python.pathlib_path_predicates`

- Verdict: `unpriced` — Observed Path usage does not imply a pure semantic row.
- Proposed pack: `nose.python.stdlib.pathlib.predicates`
- Row slice: Path.exists/is_file/is_dir style predicates
- Corpus signal: 10 repos, 5 held-out repos, 436 raw occurrences
- Current detector/query result: Current exact semantic channel is not allowed to depend on filesystem state.
- Product query sample: sample product query overlay plus existing semantic-pack inventory knowledge; candidate pack observed: False
- Impact: Medium prevalence in Python repos, but many uses are filesystem effects rather than refactoring equivalence.
- Safety: High: filesystem state and symlink/error behavior are external effects.
- Hard negatives: exists/is_file/is_dir hit filesystem, symlink behavior, permissions/errors, shadowed Path
- Rejection context: Observed Path usage does not imply a pure semantic row.
- Samples:
  - `black:src/black/__init__.py:779` (pathlib_predicates) — set of files to be formatted.""" sources: set[Path] = set() assert root.is_absolute(), f"INTERNAL ERROR: `root` must be absolute but is {root}" using_default_exc
  - `black:src/black/__init__.py:800` (pathlib_predicates) — e the logic here to the logic in `gen_python_files`. if is_stdin or path.is_file(): if resolves_outside_root_or_cannot_stat(path, root, report):
  - `black:src/black/__init__.py:827` (pathlib_predicates) — ut source: "{path}"', fg="blue") sources.add(path) elif path.is_dir(): path = root / (path.resolve().relative_to(root)) if ve

### 16. `python.itertools_chain_factories`

- Verdict: `priced-but-blocked` — Impact exists, but exact influence is blocked by the recorded proof/evidence prerequisite.
- Proposed pack: `nose.python.stdlib.itertools.chain`
- Row slice: itertools.chain and chain.from_iterable identity/flattening slices
- Corpus signal: 13 repos, 7 held-out repos, 137 raw occurrences
- Current detector/query result: Current collection factories and iterator identity adapters do not model Python itertools laziness.
- Product query sample: sample product query overlay plus existing semantic-pack inventory knowledge; candidate pack observed: False
- Impact: Medium in utility-heavy Python repos; may recover collection flattening idioms.
- Safety: Medium-high: iterator laziness, one-shot iterators, and consumption side effects are demand-sensitive.
- Hard negatives: generator side effects, one-shot iterator reuse, shadowed chain, mixed strings/bytes flattening
- Blocked by: iterator demand proof, source iteration effect proof
- Samples:
  - `black:src/black/lines.py:488` (itertools_chain) — }" res += "".join(str(leaf) for leaf in leaves) comments_iter = itertools.chain.from_iterable(self.comments.values()) comments = [str(comment) for comment in comments
  - `black:tests/optional.py:93` (itertools_chain) — gs = config.getoption("run_optional") if passed_args: ot_run.update(itertools.chain.from_iterable(a.split(",") for a in passed_args)) ot_run |= {no(excluded) for excluded in
  - `boltons:boltons/cacheutils.py:723` (itertools_chain) — epeaters = itertools.starmap(itertools.repeat, self.iteritems()) return itertools.chain.from_iterable(repeaters) def most_common(self, n=None): """Get the top *n* keys a

### 17. `javascript.node_path_normalization`

- Verdict: `unpriced` — Platform/cwd sensitivity dominates current value.
- Proposed pack: `nose.javascript.ecosystem.node.path_normalization`
- Row slice: path.join/resolve/normalize slices
- Corpus signal: 15 repos, 5 held-out repos, 2337 raw occurrences
- Current detector/query result: Current JS semantic packs do not model Node platform path semantics.
- Product query sample: sample product query overlay plus existing semantic-pack inventory knowledge; candidate pack observed: False
- Impact: Medium in tooling repos, but semantic equality is platform-sensitive.
- Safety: High: OS path separator, cwd, drive letters, symlinks, and absolute path rules affect behavior.
- Hard negatives: Windows vs POSIX, absolute path reset, cwd-dependent resolve, URL/fileURL conversion, shadowed path
- Rejection context: Platform/cwd sensitivity dominates current value.
- Samples:
  - `antlr4:runtime/JavaScript/webpack.config.js:13` (node_path_js) — n", entry: `./src/antlr4/index.${platform}.js`, output: { path: path.resolve(__dirname, 'dist'), filename: `antlr4.${platform}.${extensions}`,
  - `antlr4:runtime/JavaScript/webpack.config.js:25` (node_path_js) — s: [{ test: /\.js$/, exclude: [ /node_modules/, path.resolve(__dirname, "src/FileStream.js") ], use: [ 'babel-loader' ]
  - `axios:examples/server.js:21` (node_path_js) — onst file = files[i]; if (file[0] !== '.') { const stat = fs.statSync(path.join(root, file)); if (stat.isDirectory()) { dirs.push(file); }

### 18. `go.stdlib.maps_helpers`

- Verdict: `priced-but-blocked` — Impact exists, but exact influence is blocked by the recorded proof/evidence prerequisite.
- Proposed pack: `nose.go.stdlib.maps_helpers`
- Row slice: maps.Clone/Keys/Values/Equal slices
- Corpus signal: 8 repos, 4 held-out repos, 38 raw occurrences
- Current detector/query result: Current Go namespace calls cover selected fmt/strings/slices, not maps helpers.
- Product query sample: sample product query overlay plus existing semantic-pack inventory knowledge; candidate pack observed: False
- Impact: Medium: Go map helpers can map to existing map/key-view protocols when present.
- Safety: Medium-high: Go version policy, map iteration order, comparability, and mutation boundaries matter.
- Hard negatives: custom maps package, non-comparable values for Equal, mutation during iteration, order-sensitive Keys/Values consumer
- Blocked by: Go version proof, map iteration order boundary
- Samples:
  - `delve:pkg/config/config.go:463` (go_maps_helpers) — ") fmt.Fprint(w, "-------|------------\n") for _, name := range slices.Sorted(maps.Keys(Documentation)) { switch name { case "substitute-path": fmt.Fprint(w, "su
  - `delve:pkg/dwarf/frame/table.go:345` (go_maps_helpers) — Rule: RuleRegister} } func rememberstate(frame *FrameContext) { clonedRegs := maps.Clone(frame.Regs) frame.rememberedState.push(rowState{cfa: frame.CFA, regs: clonedReg
  - `etcd:pkg/featuregate/feature_gate.go:173` (go_maps_helpers) — p.Logger) MutableFeatureGate { if lg == nil { lg = zap.NewNop() } known := maps.Clone(defaultFeatures) f := &featureGate{ lg: lg, featureGateName:

### 19. `java.apache_commons_lang_string_utils`

- Verdict: `priced-but-blocked` — Impact exists, but exact influence is blocked by the recorded proof/evidence prerequisite.
- Proposed pack: `nose.java.ecosystem.commons_lang.string_utils`
- Row slice: StringUtils.isBlank/isEmpty/defaultString/equals style rows
- Corpus signal: 1 repos, 0 held-out repos, 130 raw occurrences
- Current detector/query result: Current Java string/null facts do not admit Apache Commons selectors.
- Product query sample: sample product query overlay plus existing semantic-pack inventory knowledge; candidate pack observed: False
- Impact: Medium potential, but current pinned corpus only shows own-source dev occurrences; external consumer breadth is missing.
- Safety: Medium: package/import identity and null/whitespace semantics are precise but need hard negatives.
- Hard negatives: project-local StringUtils, Junit/platform StringUtils, wrong commons package/version, locale/case-sensitive helpers
- Blocked by: external consumer corpus evidence, candidate-specific current miss/query evidence
- Samples:
  - `commons-lang:src/main/java/org/apache/commons/lang3/CharSetUtils.java:136` (commons_string_utils) — static boolean isEmpty(final String str, final String... set) { return StringUtils.isEmpty(str) || deepEmpty(set); } /** * Takes an argument in set-syntax, s
  - `commons-lang:src/main/java/org/apache/commons/lang3/CharUtils.java:338` (commons_string_utils) — static char toChar(final String str, final char defaultValue) { return StringUtils.isEmpty(str) ? defaultValue : str.charAt(0); } /** * Delegates to {@link C
  - `commons-lang:src/main/java/org/apache/commons/lang3/CharUtils.java:371` (commons_string_utils) — public static Character toCharacterObject(final String str) { return StringUtils.isEmpty(str) ? null : Character.valueOf(str.charAt(0)); } /** * Converts t

### 20. `rust.itertools_collect_vec`

- Verdict: `priced-but-blocked` — Impact exists, but exact influence is blocked by the recorded proof/evidence prerequisite.
- Proposed pack: `nose.rust.ecosystem.itertools.collect_vec`
- Row slice: Itertools::collect_vec and selected identity adapters
- Corpus signal: 2 repos, 1 held-out repos, 9 raw occurrences
- Current detector/query result: Current Rust std iterator identity adapters do not include external itertools trait methods.
- Product query sample: sample product query overlay plus existing semantic-pack inventory knowledge; candidate pack observed: False
- Impact: Low-medium: actual collect_vec calls appear in a few repos; imports alone are not counted as row occurrences.
- Safety: Medium-high: trait import, adapter chain demand, allocation, and custom trait ambiguity need proof.
- Hard negatives: custom collect_vec trait, effectful iterator, consumption order, non-Vec collection semantics
- Blocked by: Rust trait identity proof, iterator consumption effect proof
- Samples:
  - `meilisearch:crates/meilisearch/src/search/federated/perform.rs:811` (rust_collect_vec) — son!( ScoreDetails::weighted_score_values(score.iter(), *weight).collect_vec() ), ); federation.insert( FEDERATION_EXT
  - `meilisearch:crates/milli/src/search/new/matches/mod.rs:260` (rust_collect_vec) — => { char_map.iter().map(|(original, _)| *original as usize).collect_vec() } None => tokens[i].lemma().chars().map(|c| c.l
  - `meilisearch:crates/milli/src/search/new/matches/mod.rs:262` (rust_collect_vec) — } None => tokens[i].lemma().chars().map(|c| c.len_utf8()).collect_vec(), }) .take(m.char_count) .sum() } /
